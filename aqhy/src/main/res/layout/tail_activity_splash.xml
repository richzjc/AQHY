<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white">

    <LinearLayout
            android:id="@+id/parent_view"
            android:layout_width="match_parent"
            android:layout_height="@dimen/margin_dp160"
            android:layout_alignParentBottom="true"
            android:gravity="center"
            android:orientation="horizontal">

        <ImageView
                android:layout_width="@dimen/margin_dp80"
                android:layout_height="@dimen/margin_dp80"
                android:src="@drawable/ic_launcher" />

        <ImageView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="@dimen/margin_dp30"
                android:src="@drawable/aqhy_app_name" />
    </LinearLayout>

    <com.micker.core.imageloader.WscnImageView
            android:id="@+id/img"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_above="@id/parent_view"
            android:foregroundGravity="center"
            app:placeholderImage="@color/transparent"
            app:backgroundImage="@color/transparent"
            app:overlayImage="@color/transparent"
            />
</RelativeLayout>